# Integración API futura

Esta carpeta queda reservada para la integración posterior con ASP.NET Core Web API. En este avance no se crean clientes HTTP, endpoints simulados, `fetch`, `axios` ni servicios falsos.
